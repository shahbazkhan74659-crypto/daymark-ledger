# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.env*

# Generated at build time by backend/scripts/buildVercelFunction.mjs
# (pdfkit's font-metric files, copied alongside the bundled function).
# api/index.js and api/package.json ARE committed (a placeholder + the CJS
# module-type override) — only this generated subfolder is gitignored.
/api/data/
