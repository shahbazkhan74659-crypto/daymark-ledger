VITE_APP_NAME="Daymark Ledger"

# Absolute backend URL. Leave empty for local dev (the Vite proxy handles
# relative /api requests) — set to the deployed Render backend URL in
# production, e.g. https://daymark-ledger-backend.onrender.com
VITE_API_BASE_URL=
