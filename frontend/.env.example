VITE_APP_NAME="Daymark Ledger"

# Absolute backend URL override. Leave empty everywhere — local dev uses the
# Vite proxy, and production serves frontend + backend from the same Vercel
# project/origin, so relative /api requests already resolve correctly.
VITE_API_BASE_URL=
