// Placeholder committed to satisfy Vercel's pre-build "functions" pattern
// check. backend/scripts/buildVercelFunction.mjs overwrites this file with a
// self-contained esbuild bundle of the Express app as part of the build
// command — the bundled version is what actually gets deployed.
export default undefined;
