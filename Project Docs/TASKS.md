# Current Tasks

## Active

Deploying to production (Vercel + Neon), in progress 2026-09-24 — code is deploy-ready, remaining steps require the owner to create/configure the actual external accounts. **Owner decided 2026-09-24: the live app launches empty — the admin (owner's father) will create employees himself from scratch. No data migration from local `daymark_ledger_dev` to Neon.**

- [x] Object storage: Cloudflare R2 abandoned (requires a payment card to activate even the free tier) in favor of **Cloudinary** — owner's existing account, reused via a dedicated `daymark-ledger` API key created through Claude-in-Chrome browser automation with the owner approving the pivot/verification code/activation. `CLOUDINARY_CLOUD_NAME`/`CLOUDINARY_API_KEY`/`CLOUDINARY_API_SECRET` are set in `backend/.env` for local dev; upload/signed-download/delete all verified working via throwaway scripts (deleted after use). Still need the same three values set in Vercel's dashboard at deploy time.
- [x] Create a Neon Postgres project; get its connection string. (owner)
- [x] Run `prisma migrate deploy` against Neon — schema applied successfully (8 migrations), database confirmed empty, no data restore performed per the no-migration decision.
- [ ] Run `backend/scripts/seedAdmin.ts` against Neon (with real production `ADMIN_USERNAME`/`ADMIN_PASSWORD`) once the backend is deployed and `DATABASE_URL` is live — **do not** run `prisma:seed`, it also creates 18 fake sample workers.
- [x] Push this repo to GitHub — commit `20d9910` on `origin/main` (2026-09-24, still under the Render plan at that point).
- [x] **Hosting switched from Render to Vercel (2026-09-24)** — Render's Web Service turned out to require a payment card to create even on the free tier (confirmed via two failed live Blueprint syncs and Render's own Billing page: "No card on file"); Koyeb was checked as an alternative and found to have the identical requirement. Vercel's Hobby plan doesn't require a card. Backend ported to run as a Vercel serverless function (`backend/src/app.ts` + `backend/api/index.ts` + `backend/vercel.json`, `maxDuration: 60`); `frontend/vercel.json` adds the SPA fallback rewrite. `render.yaml` deleted; the Render Blueprint and its partially-created `daymark-ledger-frontend` static site (created earlier the same session) deleted from the Render dashboard. See `DECISIONS.md`'s "Hosting platform switched from Render to Vercel" entry, including the real worst-case report-generation benchmark (2.39s PDF / 0.95s Excel) that reopened and resolved the original concern about Vercel's function execution-time limits, and the accepted risk that Vercel Hobby's terms restrict it to non-commercial use.
- [ ] Push this repo to GitHub again (the Vercel-switch commit).
- [ ] Create two Vercel projects from this repo (frontend: root directory `frontend`; backend: root directory `backend`), fill in env vars in each project's dashboard: backend needs `CORS_ORIGIN`, `DATABASE_URL`, `ADMIN_USERNAME`, `ADMIN_PASSWORD`, `CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET`, `NODE_ENV=production`; frontend needs `VITE_API_BASE_URL` (set to the backend project's assigned URL) and `VITE_APP_NAME`.
- [ ] Verify end-to-end against the live deployed URLs: login, a full worker round trip, document upload/download, a report generation.

## Next

Open items carried over from the discussion phase, not yet decided or scoped into a phase:

- ~~Choose a PaaS hosting platform for the backend/database~~ — resolved 2026-09-22: **Render** (Web Service for the Express backend, Static Site for the built frontend) + **Neon** (managed Postgres), free tier, matching the owner's already-proven Render+Neon pattern from a separate project; see `DECISIONS.md`'s "Production hosting platform" entry.
- ~~Resolve document-storage persistence on Render's free tier~~ — resolved 2026-09-24: swapped `backend/src/lib/storage.ts` from local disk to Cloudinary object storage (after an abandoned first attempt at Cloudflare R2 — see `DECISIONS.md`'s two storage-decision entries). The remaining work is purely the live account/deploy steps tracked above under Active.
- Itemize the exact fields for a worker's "Personal info" (e.g. address, ID number, emergency contact — not yet itemized).
- Decide which document *types*/categories to support for worker document uploads (ID proofs, contracts, etc. — not yet itemized). Phase 15b's file-*format* allow-list (pdf/jpg/jpeg/png) is a separate technical upload-validation detail and does not resolve this — `WorkerDocument` has no category/label field yet.
- ~~Decide Reporting's actual scope~~ — resolved 2026-09-21: the owner directed building the Reporting frontend to exactly match the UI prototype (format-choice screen + config screen with Date Range and a 10-field checklist, mock Save with no persistence); see `PHASES.md`'s Phase 17 entry. ~~Phase 18 (Reporting Panel Backend) remains open~~ — resolved 2026-09-21, see Completed below.
- ~~Decide Settings' actual scope~~ — moot as of 2026-09-21: the owner cancelled the Settings panel outright and removed Phase 19/20 (Settings Panel Frontend/Backend) from the roadmap; see `PHASES.md`'s top-of-file note and `DECISIONS.md`'s "Settings panel cancelled" entry. Its former quick-actions menu entry point was already replaced by Inactive Employees earlier the same day.
- Decide whether overtime, bonuses, or deductions beyond attendance/advance are in scope.
- The login screen's "Remember Me" checkbox and "Forgot Password?" line (added when the login screen's visual design was rebuilt to match an owner-supplied mobile mockup) are currently **visual-only** — no backend behavior exists for either (the session is already a flat 30-day cookie with nothing for "remember me" to toggle, and there's no password-reset endpoint/flow). Decide whether to wire them up (e.g. a shorter session when unchecked, a real reset flow) or remove them, in a future phase.
- ~~[Phase 19] Full end-to-end testing~~ — resolved 2026-09-22, see Completed below. The automated test suite (Vitest/Supertest + Playwright) discussed alongside it remains unbuilt and is **not yet scheduled into a numbered phase** — a future task, tracked in `DECISIONS.md`'s Phase 19 testing-approach entry.

## Blocked

None.

## Completed

- [x] [UI polish] Reordered the home screen's floating quick-actions menu to Create New Employee, Manage Employees, Inactive Employees, Reporting (previously Manage Employees, Reporting, Inactive Employees, Create New Employee); navigation is keyed by each action's `key`, not array position, so no behavior changed. Added a two-layer shadow ("shadow effect + backlighting") to the app's repeated white card pattern (21 instances across `AdvanceHistoryList.tsx`, `AttendanceCalendar.tsx`, `EarningsSummaryCard.tsx`, `RepaymentBucketCard.tsx`, `WorkerRow.tsx`, `ManageWorkerRow.tsx`, the Manage/Reporting/Create-Employee cards, etc.) — a dark elevation shadow plus a soft brand-teal ambient glow behind the card, both kept low-opacity per explicit request (bumped once after an initial pass: dark shadow 8%→12%, teal glow 12%→18%). Buttons/FAB/badges/the confirm-delete modal were left untouched since they already have their own deliberate colored shadows. `npm run build` compiles cleanly on `frontend/` — 2026-09-23.
- [x] [Phase 20a/20b refinement] Fixed two real bugs and added a small feature/wording change found during actual live use (the original Phase 20a/20b completion below was only route/navigation-tested, not a full save round-trip): (1) `AdvanceDetailsScreen` had no worker-identifying header at all — added one matching `WorkerDetailScreen`'s exact pattern (back button, avatar, worker name), which required a new `GET /api/workers/:id` fetch on that screen; (2) `POST /api/workers/:id/repayments` never returned the recomputed `overview`, so a successful Save set `overview` to `undefined` and the whole screen fell into its "No data available" empty state — looked like a broken navigation; fixed by having the backend recompute and return `overview` alongside `repayment`; (3) added a small green "fully repaid" checkmark badge on a bucket's "Advance Taken" tile once its `outstanding` hits `0`, on both the Advance Details page (`RepaymentBucketCard`) and the Worker Detail page (`EarningsSummaryCard`'s month/year advance tiles, via a new `advance-overview` fetch on `WorkerDetailScreen`); (4) renamed the "Outstanding" label (and its overpayment error message) to "Pending" per owner request. `npm run build` compiles cleanly on both `backend/` and `frontend/` after each fix — 2026-09-23 (see `PHASES.md`'s Phase 20b post-completion refinement, `ARCHITECTURE.md`).
- [x] [Phase 20a/20b] Advance repayment tracking: a tag-based `AdvanceRepayment` model (`RepaymentBucket` enum, MONTH/YEAR, permanently tagged at creation) tracking repayments against two independent buckets, netted into `remainingOwed` everywhere it's calculated (Worker Detail, Reporting, the new advance-details page). New `backend/src/lib/repayment.ts` (`computeAdvanceOverview()`) and `backend/src/routes/repayments.ts` (`GET .../advance-overview`, `GET .../repayments/:bucket`, `POST .../repayments` with overpayment blocking). Frontend: a clickable "REMAINING OWED" tile on Worker Detail links to a new `AdvanceDetailsScreen` (`/workers/:id/advance-details`) with per-bucket `RepaymentBucketCard`s (advance taken/outstanding, an amount input + Save, `CollapsibleHistory`). Combined backend+frontend in one pass per the owner's detailed live-usage spec, following the Phase 9/10 precedent — 2026-09-23 (see `PHASES.md`, `DECISIONS.md`'s "Phase 20a/20b design implemented" entry, `ARCHITECTURE.md`).
- [x] [Phase 19] Full end-to-end testing, closing out the whole Phase 0–19 roadmap. A single continuous manual/live QA pass (Claude-in-Chrome + direct HTTP requests, no new test framework — matching every prior phase's verification style) walked a throwaway worker through creation, backfilled multi-status attendance, cross-month advances, personal-info editing, an Active→Inactive→Active reactivation (history confirmed intact), document upload/download/remove, two report generations (PDF + Excel, including a Field Preference), and finally deactivation + permanent deletion (cascade confirmed) — plus a full HTTP validation sweep across every endpoint and an explicit re-confirmation of all 7 `ARCHITECTURE.md` invariants together, not just per-feature. **Found and fixed a real bug along the way:** applying or creating a Reporting "Field Preference" was resetting the config screen's already-chosen Date Range and Employee selection back to empty (local component state lost on the route change into/out of the preferences sub-flow); fixed by threading a `draft` object through `location.state` across `ReportConfigScreen.tsx`/`ReportFieldPreferencesScreen.tsx`/`CreateReportPreferenceScreen.tsx`, the same pattern already used for the field checklist itself. `npm run build` compiles cleanly on `frontend/` after the fix. The automated test suite (Vitest/Supertest + Playwright, against a dedicated `daymark_ledger_test` database) was scoped alongside this but deliberately not built as part of Phase 19 itself — see the Next section above and `DECISIONS.md`. — 2026-09-22 (see `PHASES.md`, `DECISIONS.md`).
- [x] [Worker lifecycle reversal] Replaced the home screen's "Settings" quick-actions entry with "Inactive Employees" (`InactiveEmployeesScreen.tsx`, `/manage/inactive`) — lists only Inactive workers with a per-row delete button. Deleting a worker is now real and permanent (previously an absolute non-goal): `POST /api/workers/:id/delete` (`workers.ts`) 400s unless the worker is currently `INACTIVE`, otherwise `prisma.worker.delete()` cascades their Attendance/Advance/WorkerDocument rows and the route best-effort-removes their `uploads/<id>/` directory from disk. The frontend gates this behind an in-app confirmation modal (name-specific warning, explicit "Delete Permanently" button, no browser `confirm()`) since there's no undo. This is an explicit owner-directed reversal of the "workers are never deleted" rule — the owner was asked to confirm the cascading/irreversible consequence before implementation, and confirmed explicitly; `PROJECT.md`, `ARCHITECTURE.md`, and `DECISIONS.md` were all updated to record the reversal (Active workers still can't be deleted directly — deactivation remains mandatory first). Verified end-to-end: direct HTTP requests confirmed deleting an ACTIVE worker 400s, a nonexistent id 404s, unauthenticated 401s, and deleting a real INACTIVE throwaway worker (with real attendance/advance data) actually removed it from `GET /api/workers/:id` (404 after) and `GET /api/workers/all`; a live browser check confirmed the FAB menu shows "Inactive Employees" in Settings' old slot, the list correctly shows only Inactive workers, the confirmation modal renders the correct name/warning, and confirming actually removes the row and decrements the count. `npm run build` compiles cleanly on both `backend/` and `frontend/` — 2026-09-21 (see `PHASES.md`, `DECISIONS.md`).
- [x] [Phase 18 refinement] Reporting config screen defaults now start with nothing selected (Fields to Include and the employee picker both previously had pre-checked defaults); Save now redirects to `/reports` (format-choice) instead of home. Added a **Field Preference** feature scoped to the Fields to Include section only: a new "Field Preference" row on the config screen opens `ReportFieldPreferencesScreen.tsx` (`/reports/:format/preferences`, lists saved presets, "+" top-right opens `CreateReportPreferenceScreen.tsx` at `/reports/:format/preferences/new` — name + the same 10-checkbox card); saving or selecting a preference applies its fields to the config screen via React Router location state and returns there. Backend: new `ReportFieldPreference` Prisma model (`fields` as `Json`, migration `add_report_field_preference`) and `GET`/`POST /api/workers/report-preferences` on the existing `reportsRouter` — no update/delete endpoint, by design (see `DECISIONS.md`). Verified end-to-end via direct HTTP requests and a live browser flow (create → auto-applied to config screen; list → select existing → correctly swaps the checklist). Test preferences removed afterward via a throwaway script. `npm run build` compiles cleanly on both `backend/` and `frontend/` — 2026-09-21 (see `PHASES.md`, `DECISIONS.md`).
- [x] [Phase 18] Built the real Reporting panel backend: `POST /api/workers/report` (`backend/src/routes/reports.ts`) generates and streams back a PDF (`pdfmake`) or Excel (`exceljs`) file styled as a traditional bordered attendance ledger — dates 1-31 as header columns with P/H/A marks below (only when the "Attendance" field is checked; the bordered-grid table styling holds either way), one full ledger table per calendar month a multi-month date range touches. Added `backend/src/lib/reportColumns.ts`/`reportData.ts`/`reportPdf.ts`/`reportExcel.ts` and a local `backend/src/types/pdfmake.d.ts` ambient declaration (the published `@types/pdfmake` targets an incompatible older API). Rewrote `ReportConfigScreen.tsx`'s `handleSave()` (previously a Phase 17 no-op) to call the new endpoint via a new `postBlob()` helper in `lib/api.ts` and trigger a real browser download, with inline validation/loading state. Fixed a router-mount-order bug found during verification (`reportsRouter` had to be mounted before `workersRouter` in `index.ts`, or `workersRouter`'s `POST /:id` route silently swallowed `POST /api/workers/report`). Verified end-to-end: all backend validation edge cases via direct HTTP requests; a single-month PDF/Excel with all fields; the attendance field off (day-columns removed, ledger styling retained); a 3-month-spanning range (3 correctly-clamped sections, `remainingOwed` correctly identical across all three); a full ~70-worker run; a zero-activity worker; and a live browser check of the download, loading state, and inline error. `npm run build` compiles cleanly on both `backend/` and `frontend/` — 2026-09-21 (see `PHASES.md`, `DECISIONS.md`).
- [x] [Phase 17] Built the real Reporting panel frontend: `ReportingScreen.tsx` (format-choice screen at `/reports` — PDF/Excel option cards) and `ReportConfigScreen.tsx` (config screen at `/reports/:format` — Date Range fields + a 10-field "Fields to Include" checklist with the prototype's exact defaults + Cancel/Save), both `RequireAuth`-wrapped in `App.tsx`, matching the UI prototype's design and behavior exactly per the owner's explicit "exactly as prototype" direction — built first into `Prototype/project/Main.dc.html`/the live Artifact, then ported to real React/Tailwind. Added `types/report.ts` (`ReportFormat`, `ReportFieldKey`, `REPORT_FIELD_DEFS`, `DEFAULT_REPORT_FIELDS`). Wired the home screen's "Reporting" quick-actions entry (previously a no-op) to `navigate("/reports")`. Frontend-only — Save is a mock no-op that navigates home with no persistence and no backend call, matching the prototype's own `saveReport()`; Phase 18 remains the real backend phase. Verified end-to-end in a live browser: FAB → Reporting → format-choice screen → PDF card → config screen with correct default-checked fields → Cancel → back to format-choice (not home) → Excel card → freshly-reset config state. `npm run build` compiles cleanly — 2026-09-21 (see `PHASES.md`).
- [x] [Phase 14 refinement] Extended the Create New Employee document field and `ManageDocumentsCard` to support multiple documents: `CreateEmployeeScreen.tsx`'s field is now "Documents (optional)" holding a `File[]`, each with its own removable chip, uploaded in parallel after worker creation; `ManageDocumentsCard.tsx`'s file input gained `multiple`, uploading each file in sequence (its `onChange` prop widened to `React.Dispatch<React.SetStateAction<...>>` to support functional updates). `npm run build` compiles cleanly — 2026-09-20 (see `PHASES.md`).
- [x] [Phase 14 refinement] Added an optional "Document (optional)" field to `CreateEmployeeScreen.tsx`, matching `ManageDocumentsCard`'s picker styling/file-type allow-list. Since documents require an existing worker id, the file stays in local state until submit; `handleSubmit` creates the worker first, then best-effort uploads the file (via `postForm()`) using the new id, logging (not blocking) on upload failure. Verified end-to-end in a live browser: file chip UI works, the created worker's document appeared correctly on its Manage Employees Details page and persisted across reload, zero console errors — 2026-09-20 (see `PHASES.md`). Test worker + uploaded file removed afterward via a throwaway script.
- [x] [Phase 12 refinement] Added a read-only `PersonalInfoCard.tsx` (Name/Designation/Contact/Joining date, plus a Documents chip row) above the calendar on the Worker Detail attendance screen, reusing/extending the screen's existing data fetch (added `GET /api/workers/:id/documents`, no new type needed) — editing/uploading/removing still remains exclusive to the Manage Employees Details page, per the one-canonical-edit-path rule already used for per-day rate. Tapping a document chip opens a new `DocumentPreviewModal.tsx` (images inline via `<img>`, PDFs via a new custom canvas-based `PdfViewer.tsx` built with `pdfjs-dist`, dynamically imported to keep it out of the main bundle) — built after an initial `<iframe>`-based approach left an unremovable native-browser-PDF-viewer scrollbar that no CSS or URL-fragment trick could suppress. Also changed the backend's document-download endpoint's `Content-Disposition` from `attachment` to `inline` so files render instead of force-downloading, and tuned the document chip's color to the app's brand teal. Verified live in a browser: card renders correctly with a real document chip, the preview modal opens and renders both an image and a real PDF correctly via the custom canvas viewer with working page-nav, zero console errors — 2026-09-20 (see `PHASES.md`, `DECISIONS.md`'s "Personal Information shown (read-only)" and "Custom canvas-based PDF viewer" entries).
- [x] [Phase 16b] Built the real Manage Employee Details screen (`ManageEmployeeDetailScreen.tsx`, replacing the `ManageEmployeeStub.tsx` placeholder, now deleted) with `ManagePersonalInfoCard.tsx` (save-on-blur personal-info form, bundling all four fields per `POST /api/workers/:id`) and `ManageDocumentsCard.tsx` (real file upload/list/download/remove via a new `postForm()` helper and the Phase 15b documents endpoints). The header's Active/Inactive pill button toggles instantly via `POST /api/workers/:id/status`, matching the UI prototype exactly, no confirmation dialog. Extended `WorkerDetail` with `contact`/`joiningDate`; added `WorkerDocument` type and `formatFileSize()`. Verified end-to-end in a live browser: field edit persists on blur (and reverts on empty/invalid), status toggle persists both directions, document upload/download-link/remove all work and persist, rejected uploads show inline errors, the List screen reflects edits immediately on back-navigation, unknown-worker shows a not-found state, zero console errors. `npm run build` compiles cleanly — 2026-09-20 (see `PHASES.md`). This completes the Manage Employees feature end-to-end (Phases 15a/15b/16a/16b).
- [x] [Phase 16a] Built the real Manage Employees List screen: `ManageEmployeesScreen.tsx` (back-button header, "Workers (N)" label, fetches `GET /api/workers/all`) and `ManageWorkerRow.tsx` (avatar + name/designation + Active/Inactive badge, whole row links to a stub detail route), matching the UI prototype's design exactly. Added `ManageEmployeeStub.tsx` (minimal placeholder, no data fetch) at a new `/manage/workers/:id` route, and `/manage/workers` for the list, both `RequireAuth`-wrapped in `App.tsx`. Wired the home screen's "Manage Employees" quick-actions entry to navigate there. Added `WorkerStatus`/`ManageWorker` types and four new Active/Inactive theme tokens. Verified end-to-end in a live browser: FAB → Manage Employees → correct 20-worker roster with correct badges (including both Inactive seed workers) → tapping a row → stub → back → list → back → home (still showing the unaffected 18-Active-only home list) — zero console errors throughout. `npm run build` compiles cleanly — 2026-09-20 (see `PHASES.md`). No editing, Active/Inactive toggling, or document management UI exists yet — Phase 16b.
- [x] [Phase 15b] Extended `GET /api/workers/:id` to also return `contact`/`joiningDate`, and added `POST /api/workers/:id` (update `fullName`/`designation`/`contact`/`joiningDate`, excluding `perDayRate`/`status` which have their own endpoints), `POST /api/workers/:id/status` (explicit `{status: "ACTIVE"|"INACTIVE"}` toggle), and a new `backend/src/routes/documents.ts` (`POST`/`GET /:id/documents`, `GET /:id/documents/:documentId/download`, `POST /:id/documents/:documentId/remove`) backed by a new `WorkerDocument` model and local-filesystem storage via `multer` (see `DECISIONS.md`'s new storage decision) — resolving the previously-open storage-mechanism item above. Verified end-to-end via direct HTTP requests behind session auth: update-info and status-toggle worked correctly with proper 400/404 handling, toggling `seed-worker-1` Inactive then Active left their attendance/advance/salary-summary history byte-identical throughout, and a full document upload → list → download (checksum-verified) → remove round trip worked with rejected uploads (wrong type, oversized, missing file) correctly returning 400 not 500. `npm run build` compiles cleanly — 2026-09-20 (see `PHASES.md`). No Manage Employees frontend exists yet — Phase 16a/16b.
- [x] [Phase 15a] Added `GET /api/workers/all` to `backend/src/routes/workers.ts` (behind `requireSession`, registered before the existing `GET /:id` route to avoid being shadowed by it) — returns every worker regardless of Active/Inactive status (`id`/`fullName`/`designation`/`status` per worker, sorted alphabetically by name, `contact`/`joiningDate`/`perDayRate` deliberately omitted for Phase 15b), distinct from Phase 9's active-only `GET /api/workers`, for the future Manage Employees List screen. Verified end-to-end via direct HTTP requests behind a real login session: `401` unauthenticated; authenticated response returned all 20 workers currently in the database (18 Active + 2 Inactive — the database has grown past the original 8-seed-worker figure, now including two real workers added via the working Create Employee flow) vs. `GET /api/workers`'s 18-Active-only result. `npm run build` compiles cleanly — 2026-09-20 (see `PHASES.md`). No single-worker detail retrieval, editing, status toggling, document management, or frontend exist yet — Phase 15b and Phase 16a respectively.
- [x] [Phase 14] Built the real Create New Employee form (`CreateEmployeeScreen.tsx`, a full-screen form reusing `WorkerDetailScreen`'s header/`SalaryConfigCard`'s card/`DayEditPopup`'s button styling) at a new `/workers/new` route, wired to `POST /api/workers` (Phase 13) via the existing `postJson`/`ApiError` pattern established by `LoginScreen`. Wired only the home screen's "Create New Employee" quick-actions entry to navigate there; Manage Employees/Reporting/Settings remain untouched placeholders. Added `CreateWorkerInput`/`CreatedWorker` types to `types/worker.ts`. Verified end-to-end in a live browser: FAB → form → disabled Save until all 5 fields valid → submit redirects to `/` with the new worker listed immediately; Cancel discards without creating; the other three menu entries still no-op; zero console errors. Test worker removed afterward via a throwaway script (no delete endpoint exists by design). `npm run build` compiles cleanly — 2026-09-20 (see `PHASES.md`). No Manage Employees, Reporting, or Settings screens exist yet.
- [x] [Phase 13] Added `POST /api/workers` (full name, designation, contact, joining date, per-day rate; always created Active, status not client-selectable) to `backend/src/routes/workers.ts` behind `requireSession`, reusing the existing manual-validation, Decimal/date-serialization, and error-handling conventions already established in that file — no schema migration needed (Phase 9's `Worker` model already had every required field). Verified end-to-end via direct HTTP requests behind a real login session (401 unauthenticated, 400 on each missing/invalid field, 200 with full worker data on success, new worker confirmed via both the list and single-worker endpoints, test worker removed afterward via a throwaway script since no delete endpoint exists by design) — 2026-09-20 (see `PHASES.md`). No Create New Employee frontend form exists yet — that's Phase 14.
- [x] [Phase 12 refinement] Made the Worker Detail day-popup's P/H/A picker (`DayEditPopup.tsx`) toggle off on a second tap of the already-selected status, clearing that day's attendance on Save (via the existing `status: null` clear path already used by `setAttendanceForDate()`/the home screen's status pills) — previously tapping a selected status again did nothing. Verified live: tapping a marked day pre-selects its status as before, tapping that same status again deselects all three options, saving deletes the day's `Attendance` row (confirmed via a hard reload), and re-selecting/saving restores it — zero console errors — 2026-09-20 (see `PHASES.md`'s Phase 12 addenda).
- [x] [Phase 11/12 refinement] Made the Worker Detail "Earnings & Advances" stats month-scoped: `GET /api/workers/:id/salary-summary` now takes `year`/`month` query params (defaulting to the real current month/year), gross/net earned and this-month advance are scoped to that month, this-year advance follows that month's year, and remaining owed stays all-time — reversing Phase 11's original all-time-totals decision per the owner's explicit request after reviewing the live calendar. `WorkerDetailScreen.tsx` now refetches salary-summary on every calendar month-nav instead of once on mount; `EarningsSummaryCard.tsx` shows a "{Month} {Year}" subtitle for clarity. Verified via direct HTTP requests (valid/missing/invalid query params) and a live browser check confirming stats update correctly across a month change while remaining-owed/this-year-advance behave as specified — 2026-09-20 (see `PHASES.md`'s Phase 11/12 addenda, `ARCHITECTURE.md`, `DECISIONS.md`'s month-scoping decision).
- [x] [Phase 12] Built the real Worker Detail screen (`WorkerDetailScreen.tsx` + `AttendanceCalendar`/`DayEditPopup`/`SalaryConfigCard`/`EarningsSummaryCard`/`AdvanceHistoryList`), replacing the `WorkerDetailStub` placeholder, wired to the Phase 11 API. Closed two backend gaps discovered during planning by adding `GET /api/workers/:id` and `GET /api/workers/:id/attendance` to `workers.ts`, and — per the owner's explicit choice — pulled per-day-rate editing forward from Phase 15b via a new `POST /api/workers/:id/rate` endpoint (rate only; both decisions recorded in `DECISIONS.md`). Extracted shared `lib/avatar.ts`/`lib/format.ts`/`lib/calendar.ts` utilities. Verified end-to-end in a live browser: calendar renders seed data correctly, day-popup opens blank on empty days, saving updates the calendar/counts/summary/history immediately and persists across a hard reload, rate editing recalculates totals live, back navigation and direct-URL reload both work, unknown-worker shows a not-found state, zero console errors — 2026-09-20 (see `PHASES.md`, `ARCHITECTURE.md`, `DECISIONS.md`). No Create New Employee, Manage Employees, Reporting, or Settings exist yet — those remain future phases.
- [x] [Phase 11] Migrated the `Advance` model (`@@unique([workerId, date])`, one advance per worker per day, mirroring `Attendance`) via `prisma migrate dev --name add_worker_advance` — the second real domain migration after Phase 9. Added `parseDateOnly()`/`dateOnlyToString()` (`backend/src/lib/date.ts`) and a pure `computeSalaryTotals()` (new `backend/src/lib/salary.ts`) applying the flat half-day rule and computing all-time gross/net earned plus month/year/all-time advance totals (translating the UI prototype's client-side calculation). Extended `backend/src/routes/workers.ts` with `GET`/`POST /api/workers/:id/attendance/:date` (any-date get/set, refactored to share a `setAttendanceForDate()` helper with the existing `/attendance/today`) and `GET /api/workers/:id/salary-summary`; added a new `backend/src/routes/advances.ts` with `POST /api/workers/:id/advances/:date` (upsert/clear) and `GET /api/workers/:id/advances` (history). Extended `backend/prisma/seed.ts` with sample Attendance/Advance rows (dated relative to the real current date) for two seed workers so the calculation endpoint has hand-verifiable data. Two decisions were confirmed with the owner during planning and recorded in `DECISIONS.md`: Advance is one-entry-per-worker-per-day (not an append-only ledger), and gross/net earned totals are all-time (not period-scoped), both matching the working UI prototype's actual behavior. Verified end-to-end via direct HTTP requests behind a real login session (401s unauthenticated, upsert-not-duplicate confirmed via a database row-count check, 400/404 on invalid input, `salary-summary` matched hand-calculated totals exactly for both seed workers, and a live-added entry confirmed the endpoint reads current database state) — 2026-09-20 (see `PHASES.md`, `ARCHITECTURE.md`, `DECISIONS.md`). No Worker Detail frontend exists yet — that's Phase 12.
- [x] [Phase 9 + Phase 10] Built together in one pass, per the owner's explicit direction (so there'd be a real working mobile screen to review, not just an API) — Phase 9's backend-only scope/completion criteria were still met in full before Phase 10 began. **Phase 9:** migrated `Worker`/`Attendance` models (`WorkerStatus`/`AttendanceStatus` enums, `@@unique([workerId, date])`) via `prisma migrate dev --name add_worker_attendance`, extended `backend/prisma/seed.ts` with 8 sample workers (7 Active, 1 Inactive — no create-worker endpoint exists yet), and built `GET /api/workers` (active workers + today's status) and `POST /api/workers/:id/attendance/today` (upsert-based) behind `requireSession`. **Phase 10:** built the real `HomeScreen` (header, worker list with inline P/H/A status pills matching the UI prototype's exact colors/layout, floating quick-actions menu with all 4 entries visible but non-functional placeholders), a `/workers/:id` stub route, and removed the superseded `HomePlaceholder`; laid out mobile-first with a centered fixed-aspect card treatment on wider viewports (matching `LoginScreen`'s established desktop-preview pattern), per the owner's "mobile view focused" direction. Verified end-to-end: backend via direct HTTP requests (401/200/400/404 cases, upsert-not-duplicate confirmed), frontend via a live browser (Claude-in-Chrome) — status-pill taps persist across reload, worker-name tap navigates to the stub, FAB open/close and all 4 placeholder items work with zero console errors. Both `npm run build`s compile cleanly — 2026-09-19 (see `PHASES.md`, `ARCHITECTURE.md`). **Post-completion refinements** (same day, owner feedback after reviewing the live screen): header simplified to logo + admin username (replacing "Daymark Ledger") + date, with a working logout button on the right (the checkmark placeholder mark was also swapped for the real `/logo.png` logo along the way); worker-row/avatar/pill/FAB sizing tightened for better mobile density; status pills can now be tapped again to un-set them (`POST .../attendance/today` accepts `status: null`, which deletes that day's record); quick-actions menu buttons gained a tuned visible border. See `PHASES.md`'s Phase 10 addendum for full detail.
- [x] [Phase 8] Built the real login screen: `react-router-dom` (`/login`, `/`), a Context-based `AuthProvider` (session-checks `/api/auth/me` on mount, exposes `login`/`logout`), a `RequireAuth` route guard, an AJAX-submitted `LoginScreen`, and a `HomePlaceholder` stub with a logout button — replacing Phase 6's `/api/db-check` demo in `App.tsx`; verified end-to-end in a live browser including reload-persists-session and logout-actually-clears-session — 2026-09-19 (see `PHASES.md`, `ARCHITECTURE.md`, `DECISIONS.md`)
- [x] Rebuilt `LoginScreen`'s visual design to match an owner-supplied mobile mockup: a navy curved-header SVG shape with the real app logo, underline-style fields, a working password show/hide toggle, a visual-only "Remember Me" checkbox and "Forgot Password?" line, and a pill LOGIN button, replacing the earlier teal centered-card design — the app-wide teal brand tokens are unchanged elsewhere (e.g. `HomePlaceholder`); functionality (login/logout/session persistence) reverified unchanged — 2026-09-19 (see `PHASES.md`, `ARCHITECTURE.md`)
- [x] [Phase 7] Migrated the `User`/`Session` schema (first real migration), seeded the admin user from `ADMIN_USERNAME`/`ADMIN_PASSWORD` env vars, and implemented `POST /api/auth/login`, `POST /api/auth/logout`, `GET /api/auth/me`, and `requireSession` middleware — bcrypt-hashed password, SHA-256-hashed session token, httpOnly cookie with a 30-day expiry; verified end-to-end via direct HTTP requests — 2026-09-19 (see `PHASES.md`, `ARCHITECTURE.md`, `DECISIONS.md`)
- [x] [Phase 6] Wired up the first end-to-end round trip: a Prisma Client singleton (`backend/src/db.ts`) plus a new `GET /api/db-check` endpoint running `SELECT NOW()` (schema still has zero models), CORS via the `cors` package (`CORS_ORIGIN` env var), a new `VITE_API_BASE_URL` frontend env var, and `App.tsx` fetching and rendering the result — replacing Phase 5's blank placeholder page, as required by this phase's completion criteria; verified with both dev servers running together via direct HTTP checks, a clean backend `npm run build`, and (Claude-in-Chrome connected this session) an actual browser load showing the fetched DB timestamp with no console errors — 2026-09-19 (see `PHASES.md`, `ARCHITECTURE.md`, `DECISIONS.md`)
- [x] [Phase 5] Scaffolded the React + Vite + TypeScript frontend at `frontend/`, added Tailwind CSS v4 via `@tailwindcss/vite`, stripped the demo content to a plain blank white placeholder page (per owner direction), and set up `frontend/.env`/`.env.example` (`VITE_APP_NAME` only — `VITE_API_BASE_URL` deferred to Phase 6); verified via `npm run dev` (HTTP checks against the dev server) and a clean `npm run build` — no backend connection, routing, or real screens yet — 2026-09-19 (see `PHASES.md`, `ARCHITECTURE.md`, `DECISIONS.md`)
- [x] [Phase 4] Added `prisma:validate`/`prisma:format`/`prisma:generate`/`prisma:migrate:status`/`prisma:studio` npm scripts to `backend/package.json` and confirmed each runs successfully against `daymark_ledger_dev` (validate/format/generate clean; migrate status correctly reports the DB as unmanaged since no migrations exist yet; studio starts, serves over HTTP, and stops cleanly); reverified Prisma Client + `@prisma/adapter-pg` compile/connect cleanly via a throwaway script, then deleted it — schema still has zero models, `prisma/migrations` still does not exist — 2026-09-19 (see `PHASES.md`, `ARCHITECTURE.md`)
- [x] [Phase 3] Installed Prisma 7.10.0 + `@prisma/client` (matched versions) in `backend/`, scaffolded a bare `prisma/schema.prisma` (zero models) plus `prisma7.config.ts`, and confirmed Prisma Client connects to `daymark_ledger_dev` via the `@prisma/adapter-pg` driver adapter (required by this Prisma version — no built-in engine binary connection) — 2026-09-19 (see `PHASES.md`, `ARCHITECTURE.md`)
- [x] [Phase 2] Scaffolded the Node/Express 5 + TypeScript backend project at `backend/` (folder structure, `tsconfig.json`, npm scripts for dev/build/start, `.env`/`.env.example` for `PORT`) with a working `GET /health` endpoint — verified via both `npm run dev` and `npm run build && npm start`; no DB/Prisma connection yet — 2026-09-19 (see `PHASES.md`, `ARCHITECTURE.md`)
- [x] [Phase 1] Confirmed the local PostgreSQL 18 server (`postgresql-x64-18` Windows service) is installed, running, and set to Automatic startup — 2026-09-19 (see `PHASES.md`, `DECISIONS.md`)
- [x] [Phase 1] Decided the dev connection approach: use the existing native Windows PostgreSQL 18 service directly, not Docker — 2026-09-19 (see `DECISIONS.md`)
- [x] [Phase 1] Created the empty development database `daymark_ledger_dev` — 2026-09-19
- [x] [Phase 1] Decided to connect as the `postgres` superuser directly for local dev, no dedicated dev role — 2026-09-19 (see `DECISIONS.md`)
- [x] [Phase 1] Confirmed a working connection string end-to-end via `psql` (`postgres@localhost:5432/daymark_ledger_dev`), with zero tables present — 2026-09-19
- [x] [Phase 0] Define purpose and users (offline PWA for a single admin — the owner's father — managing daily-wage worker attendance/salary/advances) — 2026-09-17 (see `PROJECT.md`)
- [x] [Phase 0] Define platform approach: offline-first mobile web PWA, local on-device storage, no server/hosting — 2026-09-17 (**superseded 2026-09-18** — the owner's father needs the app usable from any device, which local-only storage can't support; replaced by the production stack decision below — see `PROJECT.md`, `DECISIONS.md`)
- [x] [Phase 0] Define login approach: dedicated login screen, admin-set credentials — 2026-09-17 (see `DECISIONS.md`)
- [x] [Phase 0] Define worker lifecycle: never deleted, Active/Inactive status only, history preserved on reactivation — 2026-09-17 (see `DECISIONS.md`)
- [x] [Phase 0] Define the worker module: personal/employment info, monthly color-coded attendance calendar (editable for any date), attendance-entry/advance-salary popup, per-worker salary rate with a flat half-day rule, and auto-calculated salary/advance totals — 2026-09-17 (see `PROJECT.md`)
- [x] Set up the 6-file documentation system (`CLAUDE.md`, `PROJECT.md`, `PHASES.md`, `TASKS.md`, `ARCHITECTURE.md`, `DECISIONS.md`) in `Project Docs/`, carrying forward the owner's discussion notes — 2026-09-18
- [x] Extracted the remaining raw discussion notes (`PROJECT_SPEC.md`) fully into this documentation system and deleted the file, per the owner's request — 2026-09-18
- [x] Named the app **Daymark Ledger** — explored naming options against ChatGPT and Gemini using the same brief, converged on "Daymark" plus "Ledger" — 2026-09-18 (see `PROJECT.md`, `DECISIONS.md`)
- [x] Locked the production tech stack: React + Vite + TypeScript + Tailwind CSS (frontend), Node.js + Express + TypeScript REST API (backend, Next.js not used by default), PostgreSQL 18 via Prisma, database-backed sessions with bcrypt password hashing — chosen after the owner clarified the app needs to be usable from any device, ruling out the original offline/local-storage plan — 2026-09-18 (see `PROJECT.md`, `ARCHITECTURE.md`, `DECISIONS.md`)
- [x] Built an interactive mobile UI prototype (Claude Artifact, not production code) covering: home worker list with inline today-status change, per-worker attendance/salary/advance detail, a floating quick-actions menu, Manage Employees (Active/Inactive toggle, personal-info edit, document add/remove), Create New Employee, and placeholder Reporting/Settings screens — 2026-09-18 (source kept at `Prototype/` in this repo; see `ARCHITECTURE.md`'s Prototype note and `DECISIONS.md`)
